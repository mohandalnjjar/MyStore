import 'package:flutter/material.dart';

class ProfileViweBody extends StatelessWidget {
  const ProfileViweBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ProfileViwBody'),
    );
  }
}
